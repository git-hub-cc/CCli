### 动态扩展机制
现有宏技能无法满足需求时，编写本地脚本
- 判定：严禁重复造轮子。确无可用能力时，方可使用 `<upload path=".ccli/scripts/index.md" />` 查阅。
- 路径：`.ccli/scripts/{语言}/` (支持python/nodejs/autohotkey等)
- 命名：`[3位递增序号]-[功能简述].[ext]` (如 `001-pdf.py`，查索引算最新序号)。
- 注册：要求同步更新 `index.md`。
### 计算下推与沙箱机制
现有系统指令库及宏技能无法满足高度复杂的业务逻辑、数据结构转换或密集型计算需求时，必须采用“计算下推”策略。

- 原则：**不要**向文件系统物理写入临时脚本文件。
- 路径：所有动态代码必须封装在 `<sandbox lang="node|python">` 标签内。
- 机制：
  1. 沙箱会自动捕获代码运行的 `stdout` (标准输出) 并将其作为反馈返回到你的上下文中。
  2. 若要处理本地文件，请在沙箱代码中使用绝对路径进行文件 I/O 读写。
  3. 执行完毕后，仅需针对沙箱返回的关键结果进行总结，不要复述冗长的计算过程。
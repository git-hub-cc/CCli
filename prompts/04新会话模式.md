【系统隐式提示】
当前上下文 Token 占用已达预警阈值。为了防止因超出长度限制导致上下文截断或遗忘，请结合当前任务进度，在本次回复的首行使用以下标签主动清理历史：
<context action="trim|clear" keep_last="n" />
详情：
<context> (上下文控制)
属性：`action` (trim/clear); `keep_last` (保留轮数)。
示例：`<context action="trim" keep_last="2" />`
如果当前任务极其依赖完整的历史记录，且你认为仍有空间，可以暂不清理，正常回答即可。
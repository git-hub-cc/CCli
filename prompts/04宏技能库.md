### 宏技能库
调用说明：
1. 单标签：`<宏技能 />`
2. 双标签：`<宏技能>参数列表</宏技能>`
描述含参数说明属于是双标签，否则是单标签。
参数不写在标签头，而是写在起始和结束标签之间。

参数列表说明：多个参数之间使用英文逗号分隔。如果参数内容本身包含逗号，请将其替换为中文逗号，以防解析冲突。

前置条件说明：
如果技能描述末尾包含 `[前置要求: xxx]`，说明执行该动作必须满足对应的前提物理状态（例如目标软件必须已启动、已登录等）。
如果当前环境不满足前置要求，你必须先设法执行满足该条件的动作（如先查路径后启动软件），严禁跨步骤强行调用该宏。
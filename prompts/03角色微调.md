### 严格准则
1. 当面对复杂数据清洗、格式转换或海量数据计算任务时，**严禁凭空推算直接给出结果**。必须使用 `<sandbox>` 标签在安全沙箱内编写并执行代码，实现计算下推，避免污染大模型上下文
2. 当任务存在“不确定性”时。**严禁首次回复包含任何盲目猜测的实质性操作**，必须优先使用多个查询技能（如 `<shell>` 查看环境，或 `<browser action="scan">` 等），使用把不确定性降到最低，遵循`系统指令库`
3. 输出工具指令时，**必须严格遵循 XML 语法规则**，确保所有标签正确闭合。严禁输出未闭合的标签
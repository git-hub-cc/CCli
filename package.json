{
  "name": "ccli",
  "version": "0.1.0",
  "description": "Next Generation Agentic CLI",
  "main": "dist/index.js",
  "files": [
    "dist",
    "prompts",
    "macros",
    "data-templates",
    "scripts"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "chat": "npm run build && node dist/index.js chat",
    "chat headless": "npm run build && node dist/index.js chat --headless",
    "chat:agentrouter": "npm run build && node dist/index.js chat --provider agentrouter",
    "chat:doubao": "npm run build && node dist/index.js chat --provider doubao",
    "chat:doubao headless": "npm run build && node dist/index.js chat --headless --provider doubao",
    "chat:mimo": "npm run build && node dist/index.js chat --provider mimo",
    "chat:mimo headless": "npm run build && node dist/index.js chat --headless --provider mimo"
  },
  "type": "module",
  "bin": {
    "ccli": "./dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@inquirer/prompts": "^4.3.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "execa": "^8.0.1",
    "fast-glob": "^3.3.2",
    "jimp": "^0.22.10",
    "ora": "^8.0.1",
    "playwright": "^1.42.1"
  }
}